const initialState = {
  historyList: [],
  nextToken: null
};

const record = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default record;
